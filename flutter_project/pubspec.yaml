name: negocio_flex
description: "Negocio Flex - Plataforma Multiempresa SaaS para Pequeños y Medianos Negocios"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  supabase_flutter: ^2.6.0
  flutter_dotenv: ^5.1.0
  google_fonts: ^6.2.1
  lucide_icons: ^0.252.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
